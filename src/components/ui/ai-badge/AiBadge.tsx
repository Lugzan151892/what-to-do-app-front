import aiBadgeStyles from '@/components/ui/ai-badge/ai-badge.module.scss';

const AiBadge = () => {
  return (
    <div className={aiBadgeStyles.badge}>
      AI
    </div>
  );
};

export default AiBadge;
